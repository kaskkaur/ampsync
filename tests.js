for (let i = 0; i < 900; i++) {
    const testLog = {
        time: "time",
        id: "Ticket: " + i,
        status: " Status: OPEN ",
        response: " failed to fetch full records: ",
        isError: true
    };
    pushLog(testLog);
}


